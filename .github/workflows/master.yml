name: Nuxt.js CI

on:
  push:
    branches: [ master, dev ]
  pull_request:
    branches: [ master, dev ]

jobs:
  build:
    
    runs-on: ubuntu-latest

    steps:
    - name: Install npm dependencies
      run: |
        pwd
        ls
        npm install
    - name: Nuxt Linter
      run: |
        npm run lintfix
        npm run lint
    - name: Build Nuxt application
      run: |
        npm run build
