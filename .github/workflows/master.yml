name: Nuxt.js CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    
    runs-on: ubuntu-latest

    steps:
    - name: Install npm dependencies
      run: |
        npm install
    - name: Nuxt Linter
      run: |
        npm run lintfix
        npm run lint
    - name: Build Nuxt application
      run: |
        npm run build
